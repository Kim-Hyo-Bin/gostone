// Package app composes the gostone service (HTTP, storage, configuration).
package app

// Run starts the gostone process. It is a stub until the server is implemented.
func Run() error {
	return nil
}
